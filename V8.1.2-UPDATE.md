# V8.1.2 新增商品修正

修正 PostgreSQL 23502：固定價格商品新增/儲存時，不再把 min_amount、max_amount、amount_step 寫成 NULL。

固定價格商品會寫入：
- min_amount = 0
- max_amount = 0
- amount_step = 50

這與目前資料庫中既有固定價格商品的資料格式一致。

本版不需要新增 SQL；若已執行 V8.1.1 權限修正 SQL，直接部署 V8.1.2 即可。

# V8.1.5 後台價格輸入修正

後台價格設定改為自由整數輸入，不再把營運規則寫死在管理介面。

- 固定價格：可輸入任意 0 以上整數，例如 50、80、120、350。
- 彈性最低金額：可輸入任意 0 以上整數。
- 彈性最高金額：可輸入任意 0 以上整數。
- 每次增加：可輸入任意 1 以上整數，例如 1、10、50、80、100、150。
- 新增商品與編輯既有商品同步修正。
- 前台彈性價格仍依資料庫 `amount_step` 決定加減級距。
- 不需要新增 Supabase SQL，沿用 V8.1.3 之後的資料庫設定。

# V8.1.3 更新

修正商品新增與儲存時的 NOT NULL 防呆。

- 固定價格：price=實際價格、min_amount=0、max_amount=0、amount_step=100
- 彈性價格：price=0、min_amount=設定值、max_amount=設定值、amount_step=設定值（最低 1）
- 新增 `supabase-v8.1.3-product-defaults.sql`，替數值欄位補預設值並修正既有 NULL。

部署順序：先執行 SQL，再更新 GitHub/Vercel。

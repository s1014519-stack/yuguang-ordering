# V8.1.1 新增商品修正

1. Supabase → SQL Editor 執行 `supabase-v8.1.1-product-insert-fix.sql`。
2. 將本專案更新到 GitHub，Vercel 重新部署。
3. 登入 `/admin` → 商品管理 → 新增商品。

這版修正：
- 明確 GRANT authenticated 對 products 的 SELECT / INSERT / UPDATE / DELETE。
- products.id 預設為 gen_random_uuid()。
- products.created_at 預設為 now()。
- 前端新增商品也主動產生 UUID。
- 新增失敗時直接顯示 Supabase 的實際錯誤碼與訊息，方便立即定位。

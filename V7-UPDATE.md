# 漁光閃閃 V7 後台更新

## 本版調整
- 後台改為手機優先 POS 卡片式介面。
- 新訂單、製作中、今日訂單數字置頂。
- 訂單卡片顯示訂單編號、時間、客人、商品份數、金額與狀態。
- 訂單明細改為手機底部面板，大按鈕處理「接單並開始製作 / 完成 / 取消」。
- `accepted` 在畫面上改顯示為「製作中」，資料庫值不變，因此不影響既有資料。
- 常見 Supabase 英文錯誤改成中文提示，避免直接顯示技術訊息。
- 休息訊息只有在「休息中」才展開，營業時畫面更乾淨。
- 電話可直接點擊撥號。

## 部署
不需要新增 SQL。以 V7 覆蓋 GitHub 專案後 Push，Vercel 會重新部署。

## LINE
本版先不寫入 LINE Token，也不把敏感憑證放前端。下一階段串接 LINE Messaging API 時，會使用 Vercel 環境變數與 server-side API route。
